{
  "name": "cerberus-frontend",
  "version": "1.0.0",
  "description": "Chrome extension for phishing detection",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp manifest.json dist/ && cp -r public dist/ && cp src/popup/index.html dist/src/popup/ && cp src/popup/popup.css dist/popup/ && cp src/options/Options.html dist/src/options/ && cp src/content/overlay/overlay.css dist/content/overlay/",
    "watch": "tsc --watch",
    "pack": "sh scripts/build-and-pack.sh"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.3.0"
  }
}
